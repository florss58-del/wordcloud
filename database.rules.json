{
  "rules": {
    ".read": false,
    ".write": false,

    "instructors": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",

        "sessions": {
          "$code": {
            ".validate": "newData.hasChildren(['name', 'created'])",
            "name": { ".validate": "newData.isString() && newData.val().length <= 40" },
            "q": { ".validate": "newData.isString() && newData.val().length <= 80" },
            "mode": { ".validate": "newData.val() === 'word' || newData.val() === 'phrase' || newData.val() === 'link'" },
            "max": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5" },
            "created": { ".validate": "newData.isNumber()" },
            "$other": { ".validate": false }
          }
        },

        "$other": { ".validate": false }
      }
    },

    "sessions": {
      ".read": "auth != null",

      "$code": {
        ".read": true,
        ".write": "auth != null && data.exists() && !newData.exists()",

        "meta": {
          ".write": "auth != null && !data.exists() && newData.exists()",
          ".validate": "newData.hasChildren(['q', 'max', 'mode', 'name', 'created'])",
          "q": { ".write": "auth != null", ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 80" },
          "max": { ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5" },
          "mode": { ".validate": "newData.val() === 'word' || newData.val() === 'phrase' || newData.val() === 'link'" },
          "name": { ".write": "auth != null", ".validate": "newData.isString() && newData.val().length <= 40" },
          "created": { ".validate": "newData.isNumber()" },
          "$other": { ".validate": false }
        },

        "entries": {
          ".write": "auth != null && !newData.exists()",
          "$entryId": {
            ".write": "root.child('sessions').child($code).child('meta').exists() && root.child('sessions').child($code).child('closed').val() !== true && newData.exists() && (!data.exists() || data.child('c').val() === newData.child('c').val())",
            ".validate": "newData.hasChildren(['w', 'c', 't'])",
            "w": { ".validate": "newData.isString() && newData.val().length >= 1 && (root.child('sessions').child($code).child('meta').child('mode').val() === 'link' ? (newData.val().length <= 300 && newData.val().beginsWith('https://')) : newData.val().length <= 40)" },
            "n": { ".validate": "root.child('sessions').child($code).child('meta').child('mode').val() === 'link' && newData.isString() && newData.val().length >= 1 && newData.val().length <= 12" },
            "c": { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 64" },
            "t": { ".validate": "newData.isNumber()" },
            "$other": { ".validate": false }
          }
        },

        "hidden": {
          ".write": "auth != null",
          "$word": { ".validate": "newData.val() === true" }
        },

        "closed": {
          ".write": "auth != null",
          ".validate": "newData.isBoolean()"
        },

        "$other": { ".validate": false }
      }
    }
  }
}
